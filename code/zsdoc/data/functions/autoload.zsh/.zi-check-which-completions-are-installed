local i cfile bkpfile
reply=( )
for i in "$@"; do
  cfile="${i:t}"
  bkpfile="${cfile#_}"
  if [[ -e "${ZI[COMPLETIONS_DIR]}"/"$cfile" || -e "${ZI[COMPLETIONS_DIR]}"/"$bkpfile" ]]; then
    reply+=( "1" )
  else
    reply+=( "0" )
  fi
done
